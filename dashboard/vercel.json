{
  "build": "npm install --prefix dashboard && npm install --prefix frontend --omit=dev && npm run build --prefix dashboard && npm install --prefix cloud --omit=dev",
  "functions": {
    "api/[...path].js": {
      "includeFiles": "../cloud/**"
    }
  }
}
