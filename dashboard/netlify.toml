[build]
  command = "npm install --prefix ../cloud && npm install --prefix ../frontend --omit=dev && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200